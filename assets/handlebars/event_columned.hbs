<div class='event'>
	<h2>{{ formatDate event.start "dddd D. MMMM YYYY" }} - {{ event.title }} - Ledige skift: {{event.available}}
		{{# if user.admin }}- <a href="/admin/shifty/event/{{event.id}}/" data-bypass>Edit</a>{{/if }}
	</h2>
  	{{#each columns}}
  		<column>
			{{#each this}}
			<div class="shift {{ cssClass }} {{ shift.durationType }}{{#unless available}} taken{{/unless}} {{#if ownshift}}yourshift{{/if}}" id="shift_{{ shift.id }}">
				<div class='title'>
					{{ shift.shift_type.title }}
					{{#if hasTwins }}
						({{freeCount}}/{{twinsCount}})
					{{/if}}
				</div>
				<div class='time'>{{ formatDate shift.start "HH:mm" }}-{{ formatDate shift.stop "HH:mm" }}</div>
			</div>

			{{/each}}
		</column>
  	{{/each}}
  	<column>
  		{{#if event.description}}
		<div class='comments'>
			<div class='title'>Kommentarer</div>
			{{ event.description }}
		</div>
		{{/if}}
		{{#if event.responsible}}
		<div class='contact'>
			<div class='title'>Ansvarlig</div>
			<div>
				Navn:&nbsp;{{event.responsible.first_name}}&nbsp;{{event.responsible.last_name}}
			</div>
			<div>
				Bruker: {{event.responsible.username}}
			</div>
			<div>
				Epost: {{event.responsible.contactinfo.email}}
			</div>
			<div>
				Mobil: {{event.responsible.contactinfo.phone}}
			</div>
		</div>
		{{/if}}
	</column>		
</div>
